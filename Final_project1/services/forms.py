"""from django import forms
from .models import Personne

class personneform(forms.ModelForm):
    class Meta:
        model=Personne
        fields=('last_name','first_name','email','username','password','tel','image','role')
"""