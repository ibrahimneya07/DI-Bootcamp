# un float est un entier a virgile
# la différence est que un entier peu etre un float mais par compte un float ne peut pas etre un entier
# oui voici un exemple
import numpy
random_numbers = numpy.random.uniform(low=0, high=6, size=10).tolist()
print(random_numbers)